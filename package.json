{
    "name": "authority-layer-repo",
    "private": true,
    "workspaces": [
        "packages/core"
    ],
    "scripts": {
        "example": "npm run -w @authority-layer/core build && node examples/run.js"
    }
}